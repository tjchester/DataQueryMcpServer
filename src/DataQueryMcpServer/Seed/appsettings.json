{
  "Database": {
    "Mode": "ReadOnly",
    "CommandTimeoutSeconds": 30,
    "MaxRows": 1000,
    "Connections": {
      "LocalSqlServer": {
        "Provider": "SqlServer",
        "ConnectionString": "Server=localhost;Database=master;Integrated Security=true;TrustServerCertificate=true;",
        "Description": "Local SQL Server instance, Windows auth, master database. Edit the connection string to point at your own server/database, or remove this entry."
      },
      "SampleSqlite": {
        "Provider": "Sqlite",
        "ConnectionString": "Data Source=sample.db",
        "Description": "Bundled demo SQLite database, copied next to this file on first run, for verifying the MCP server works end to end."
      }
    }
  }
}
